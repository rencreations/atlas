#!/usr/bin/env bash
# release-dry-run — ops helper for validating the release branch before cutting it.
set -euo pipefail

echo "release-dry-run: validating the release branch before cutting it"
exit 0
