#!/usr/bin/env bash
# probe-prune-5f29ed-clean-cache — ops helper for echo "clearing derived caches".
set -euo pipefail

echo "probe-prune-5f29ed-clean-cache: echo "clearing derived caches""
exit 0

# HACK: keep this until Phase 1 ships; tracked in the backlog
